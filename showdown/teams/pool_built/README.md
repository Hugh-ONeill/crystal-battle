# pool_built — authored teams only (2026-08-04)

The authored-vs-harvested experiment: every team here was BUILT (ah*/ho*/
tr*/sun*/snow* families + future drafts), none harvested from replays, so a
ladder session on this pool measures authored-team performance uncontaminated
by the scraped majority. Seeded from the 8 authored members of the active
pool_hl (ah1/sun2 stay in pool_hl_benched — rw 0-fers, pending post-fix
re-trial). Files are COPIES: pool_hl remains the pool of record; keep edits
in sync or diverge deliberately and note it here.

Use: point the ladder session's pool arg at showdown/teams/pool_built
(overnight_ladder.sh arg 2; crystal-ladder.service ExecStart).
